{
  "name": "@evolvo/eslint-config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./base": "./base.mjs"
  },
  "scripts": {
    "build": "node --eval \"\"",
    "clean": "git clean -fdX .",
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern",
    "test": "vitest run --passWithNoTests --root . --config ../../vitest.config.mjs",
    "typecheck": "node --eval \"\""
  },
  "dependencies": {
    "@eslint/js": "^9.35.0",
    "eslint": "^9.35.0",
    "globals": "^16.4.0",
    "typescript-eslint": "^8.43.0"
  }
}
