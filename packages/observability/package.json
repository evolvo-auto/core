{
  "name": "@evolvo/observability",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./correlation-context": "./src/correlation-context.ts",
    "./logger": "./src/logger.ts",
    "./structured-event": "./src/structured-event.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "clean": "git clean -fdX .",
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern",
    "test": "vitest run --root . --config ../../vitest.config.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@evolvo/typescript-config": "workspace:*"
  }
}
