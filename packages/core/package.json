{
  "name": "@evolvo/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./config-loader": "./src/config-loader.ts",
    "./heuristic-config": "./src/heuristic-config.ts",
    "./model-routing-config": "./src/model-routing-config.ts",
    "./prompt-definition-config": "./src/prompt-definition-config.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "clean": "git clean -fdX .",
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern",
    "test": "vitest run --root . --config ../../vitest.config.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@evolvo/schemas": "workspace:*",
    "dotenv": "^17.2.2",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@evolvo/typescript-config": "workspace:*"
  }
}
