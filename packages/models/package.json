{
  "name": "@evolvo/models",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./model-routing-resolver": "./src/model-routing-resolver.ts",
    "./model-provider": "./src/model-provider.ts",
    "./ollama-provider": "./src/ollama-provider.ts",
    "./openai-provider": "./src/openai-provider.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "clean": "git clean -fdX .",
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern",
    "test": "vitest run --root . --config ../../vitest.config.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@evolvo/api": "workspace:*",
    "@evolvo/core": "workspace:*",
    "@evolvo/genome": "workspace:*",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@evolvo/typescript-config": "workspace:*"
  }
}
