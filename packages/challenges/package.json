{
  "name": "@evolvo/challenges",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./challenge-issue-draft": "./src/challenge-issue-draft.ts",
    "./normalize-challenge": "./src/normalize-challenge.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "clean": "git clean -fdX .",
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern",
    "test": "vitest run --root . --config ../../vitest.config.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@evolvo/schemas": "workspace:*"
  },
  "devDependencies": {
    "@evolvo/typescript-config": "workspace:*",
    "@types/node": "^24.5.2"
  }
}
