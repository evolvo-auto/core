{
  "name": "@evolvo/query",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./getQueryClient": "./src/getQueryClient.ts",
    "./health": "./src/health.ts",
    "./worktrees": "./src/worktrees.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "clean": "git clean -fdX .",
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern",
    "test": "vitest run --root . --config ../../vitest.config.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.90.2",
    "@evolvo/schemas": "workspace:*"
  },
  "devDependencies": {
    "@evolvo/typescript-config": "workspace:*"
  }
}
