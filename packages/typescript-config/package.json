{
  "name": "@evolvo/typescript-config",
  "version": "0.0.0",
  "private": true,
  "exports": {
    "./app.json": "./app.json",
    "./base.json": "./base.json",
    "./library.json": "./library.json"
  },
  "scripts": {
    "build": "node --eval \"\"",
    "clean": "git clean -fdX .",
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern",
    "test": "vitest run --passWithNoTests --root . --config ../../vitest.config.mjs",
    "typecheck": "node --eval \"\""
  }
}
