{
  "name": "@evolvo/memory",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./capability-tracking": "./src/capability-tracking.ts",
    "./failure-taxonomy": "./src/failure-taxonomy.ts",
    "./recurrence-clustering": "./src/recurrence-clustering.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "clean": "git clean -fdX .",
    "lint": "eslint . --max-warnings 0 --no-error-on-unmatched-pattern",
    "test": "vitest run --root . --config ../../vitest.config.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@evolvo/schemas": "workspace:*"
  },
  "devDependencies": {
    "@evolvo/typescript-config": "workspace:*"
  }
}
